{
  "experiment": "sphere_bounce_cross_scene",
  "description": "Manifest for batch PyBullet scenes with varied physics. Each scene gets its own output folder under batch_root.",
  "base_config": "configs/sphere_bounce_m2.json",
  "batch_root": "outputs/sphere_bounce_batch",
  "naming": "scene_{index:04d}_e{restitution:.2f}_m{mass_kg:.2f}_z{drop_z_m:.2f}",
  "grid": {
    "restitution": [0.45, 0.55, 0.65, 0.75, 0.85],
    "mass_kg": [0.5, 1.0, 2.0],
    "drop_z_m": [1.2, 1.6, 2.0]
  },
  "max_scenes": 100,
  "holdout": {
    "description": "Scenes with restitution 0.75 and mass 1.0 are default val; adjust in training code.",
    "restitution": [0.75],
    "mass_kg": [1.0]
  },
  "notes": [
    "Full grid is 5*3*3=45 scenes; increase grid or max_scenes for 100+.",
    "Run: python scripts/generate_sphere_bounce_batch.py --dry-run",
    "Then: python scripts/generate_sphere_bounce_batch.py"
  ]
}
